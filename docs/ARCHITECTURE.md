# Architecture

Static multipage HTML with shared CSS and small browser scripts. No framework or backend is required. Menu state is driven by one 760px media query; tips uses native dialog focus management. Instructor-provided assets remain credited. Original content is retained as historical 2022 material, not current visitor advice.
